include ../content/content
include ../container/container
include ../subtitle/subtitle
include ../heading/heading
include ../monospace/monospace
include ../personal/personal
include ../academy/academy
include ../list/list
include ../checkbox/checkbox
include ../col/col
include ../arrow/arrow
include ../tree/tree

mixin index()
	.index
		+content()
			.index__group
				+personal(getData('personalData'))

			.index__group
				+academy([
					{
						'link': 'https://htmlacademy.ru/',
						'title': 'htmlacademy.ru',
						'graduate': true
					}
				])
			.index__group
				+heading()
					| Практические навыки
				+description()
					| Отемтьте только те технологии и типы задач, с которыми вы хорошо знакомы. Отсутсвие опыта отнюдь не означает, что у вас меньше шансов стать нашим сотрудником. Это означает, что вы будете получать те задачи, с которыми гарантировано будете справляться.
				+checkbox(getData('skills'))

			.index__group
				+subtitle()
					| ЧТО ВКЛЮЧЕНО

			+heading()
				| Инструменты:

			.index__group
				+list(getData('tools'))

			.index__group
				+subtitle()
					| СТРУКТУРА ФАЙЛОВ

				+col()
					+heading()
						| Исходники
					+tree(getData('sourceTree'))

				+col()
					.index__arrow
						+arrow()
							| Компиляция

				+col()
					+heading()
						| Готовые файлы
					+tree(getData('buildTree'))
